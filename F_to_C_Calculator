def f_to_c(f):
    return 5/9 * (f - 32)
    
def test(f):
    c = round(f_to_c(f), 2)
    print(f, "degrees fahrenheit is", c, "degrees celsius")

f = input("enter fahrenheit here : ")
int_f = int(f)
test(int_f)
